/* Write a header for a function named print_a_number() that takes a single type int argument and
doesn't return anything to the calling program.*/ 

#include <stdio.h>
float print_a_number(int a);
float x;
int main()
{
printf("Give a value to x: \n ");
scanf("%d, &x");
x = print_a_number(3);
printf("His value %f", x);
return 0;
}

float print_a_number(int a)
{
return;
}
